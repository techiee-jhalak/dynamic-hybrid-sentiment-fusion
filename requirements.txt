# Dynamic Hybrid Sentiment Fusion — Production Dependencies
# Used by Docker (Railway) and any pip-based deployment for reproducible installs.
#
# CPU-ONLY PyTorch: no GPU on Railway free tier.
# The --extra-index-url below lets pip find the +cpu wheel (~200 MB vs ~2.5 GB CUDA).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
nltk>=3.8.1
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
pyyaml>=6.0
emoji>=2.8.0
fastapi>=0.100.0
pydantic>=2.0.0
uvicorn>=0.22.0
httpx>=0.24.0
vaderSentiment>=3.3.2
sentencepiece==0.2.2
